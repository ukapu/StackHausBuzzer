<!DOCTYPE html>
<html>
<head>
  <title>numbers with access</title>
  <style type="text/css" media="screen">
    td {
      padding:20px;
    }
    input {
      height:20px;
      width:20px;
    }
  </style>
</head>
<body>
  <h1>numbers with access</h1>
  <table>
    <tr>
      <th>Phone Number</th>
      <th>access</th>
    </tr>
    <% numbers.each do |number| %>
    <tr>
      <td><%= number[:number] %></td>
      <td><%= number[:active] %></td>
    </tr>
    <% end %>
  </table>
  
  <!-- Scripting Magic -->
  <script src="http://code.jquery.com/jquery-1.9.1.min.js"></script>
  <script type="text/javascript" charset="utf-8">
    $(function() {
      //Handle change 
      $('input').on('click', function(e) {
        console.log(e);
        
        //Ajax post request to kick off the SMS message
        $.ajax({
          url:'/fillOrder',
          method:'POST',
          data: {
            id:e.target.id,
            status:e.target.checked
          },
          dataType:'text',
          success: function(data, status, xhr) {
            console.log(data);
          }
        });
      });
    });
  </script>
</body>
</html>

