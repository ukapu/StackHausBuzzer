<!DOCTYPE html>
<html>
<head>
  <title>numbers with access</title>
  <style type="text/css" media="screen">
    td {
      padding:20px;
    }
    input {
      height:20px;
      width:20px;
    }
  </style>
</head>
<body>
  <h1>numbas</h1>
  <table>
    <tr>
      <th>Phone Number</th>
      <th>Date Added</th>
      <th>Admin?</th>
    </tr>
    <% numbers.each do |entry| %>
    <tr>
      <td><%= entry[:number] %></td>
      <td><%= Time.at(entry[:time_added]).localtime %></td>
      <td><%= entry[:admin] %></td>
    </tr>
    <% end %>
    
  </table>
</body>
</html>
